# secrets — never commit
.env
.env.*
!.env.example

# build / runtime artifacts
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# local index + caches + logs (rebuildable via `make ingest`)
data/
.pytest_cache/

# eval run logs (results themselves are committed under evals/results/)
data/eval-*.log
